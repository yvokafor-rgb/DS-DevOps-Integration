name: CD

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run model validation
      run: |
        python ci-cd/model_validation/validate_model.py
    
    - name: Build Docker image
      run: |
        docker build -f deployment/docker/Dockerfile -t fraud-detection-api:${{ github.sha }} .
    
    - name: Test Docker image
      run: |
        docker run -d -p 8000:8000 --name test-api fraud-detection-api:${{ github.sha }}
        sleep 5
        curl -f http://localhost:8000/health || exit 1
        docker stop test-api
        docker rm test-api
    
    # Uncomment and configure for actual deployment
    # - name: Deploy to production
    #   run: |
    #     echo "Deploy to your production environment here"

